{
  "name": "memory_graph_mcp",
  "version": "0.1.0",
  "description": "MCP server building a persistent project knowledge graph: AST dependencies, DB schema links, debug sessions, side effects",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "memory-graph-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "test": "node --test dist/test/*.test.js",
    "lint": "tsc --noEmit"
  },
  "keywords": [
    "mcp",
    "knowledge-graph",
    "code-index",
    "tree-sitter"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
